# 2-D TGV production run with TVF disabled (tvf_factor = 0).
# Format: key = value

dim = 2
nx = 50
L = 1.0
u_ref = 1.0
nu = 0.01
dt = 0.0005
t_end = 3.0

# If tvf_factor = 0.0, TVF is effectively disabled.
tvf_factor = 0.0

print_every = 200
save_every = 200
save_dir = res_tgv2d_notvf

# Optional: initialize x/u from a previously saved state file.
# Keep empty to use analytic TGV initialization.
init_state_file = res_tgv2d_init/relaxed_state.bin
