# 3D HIT
# Format: key = value

dim = 3
nx = 64
L = 6.283185307179586
u_ref = 4.0
nu = 0.002
dt = 0.001
t_end = 5

# If tvf_factor = 0.0, TVF is effectively disabled.
tvf_factor = 1.0
noise_std_factor = 0.0

print_every = 20
save_every = 100
save_dir = res/hit3d_64

# Optional: initialize x/u from a previously saved state file.
# Keep empty to use analytic TGV initialization.
init_state_file =
