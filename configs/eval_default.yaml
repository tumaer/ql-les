# @package _global_

defaults:
  - _self_
  - data: kolm2d_every1 # choose datamodule with `test_dataloader()` for evaluation
  - model: gns
  - logger: null
  - callbacks: default
  - trainer: default
  - paths: default
  - extras: default
  - vars: default
  - experiment: null
  - hydra: default

task_name: "eval"

tags: ["dev"]

ckpt_path: path/to/checkpoint.ckpt
