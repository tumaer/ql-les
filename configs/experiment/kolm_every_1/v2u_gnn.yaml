# @package _global_
defaults:
  - /experiment/kolm_every_1/base
  - override /model: v2u_gnn

vars:
  num_rollout_steps: 1

logger:
  wandb:
    project: "v2u_gnn_kolm2d_every1"

tags: ["kolm1", "v2u_gnn"]
