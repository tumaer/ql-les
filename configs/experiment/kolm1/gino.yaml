# @package _global_
defaults:
  - /experiment/kolm1/base
  - override /model: gino

model:
  _target_: src.models.gino_module.GINOLitModule
  net:
    noise_std: 0.0 # TODO: ablate

logger:
  wandb:
    project: "kolm1"

tags: ["gino"]
