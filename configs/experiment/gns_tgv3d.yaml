# @package _global_
defaults:
  - override /model: gns
  - override /data: tgv3d
  - override /trainer: gpu
  - override /vars: tgv3d
  - override /callbacks: custom
  - override /logger: wandb

data:
  batch_size: 1
  max_pushforward_steps: ${data.max_pushforward_steps}
  input_seq_length: 2

model:
  net:
    noise_std: 3.0e-4
  pushforward:
    steps: [0, 10, 700] # Training steps unlocking stages
    unrolls: #[0, 2, ${max_pushforward_steps}]   # Unroll step options for each stage
      - 0
      - 2
      - ${data.max_pushforward_steps}
    probs: [7, 2, 1] # Relative probabilities for each unroll step

tags: ["tgv", "gns"]

logger:
  wandb:
    project: "tgv3d"

seed: 12345
