# @package _global_
defaults:
  - override /model: gino
  - override /data: kolm2d_every1
  - override /trainer: gpu
  - override /vars: gino2d
  - override /callbacks: custom
  - override /logger: wandb

model:
  _target_: src.models.gino_module.GINOLitModule
  net:
    noise_std: 0.0 # TODO: ablate

tags: ["kolm", "gns"]

logger:
  wandb:
    project: "gino_kolm2d_every1"

seed: 12345
