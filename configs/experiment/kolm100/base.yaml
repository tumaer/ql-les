# @package _global_
defaults:
  - override /data: kolm2d_every100_v2
  - override /trainer: gpu
  - override /vars: kolm2d
  - override /callbacks: default
  - override /logger: wandb

model:
  net:
    metadata_path: ${data.data_dir}/${data.metadata_file}

logger:
  wandb:
    project: "kolm100"

seed: 12345
