#No pushforward
_target_: src.data.sph_datamodule.SPHDataModule
data_dir: ${paths.data_dir}/2D_KOLM_4096
batch_size: 1
num_workers: 4
input_seq_length: ${vars.input_seq_length}
max_rollout_steps: ${vars.num_rollout_steps}

# dataset_kwargs
name: kolm100
every_n: 100
metadata_file: metadata_every100.json
